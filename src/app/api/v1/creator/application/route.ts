import { z } from "zod";
import { isSupabaseConfigured } from "@/lib/supabase/config";
import { createClient } from "@/lib/supabase/server";
const schema=z.object({channelName:z.string().trim().min(2).max(80),legalName:z.string().trim().min(2).max(120),portfolioUrl:z.union([z.url(),z.literal("")]).default(""),rightsSummary:z.string().trim().min(40).max(4000)});
export async function GET(){if(!isSupabaseConfigured())return Response.json({error:"Cloud applications are unavailable"},{status:503});const supabase=await createClient();const{data:{user}}=await supabase.auth.getUser();if(!user)return Response.json({error:"Authentication required"},{status:401});const{data,error}=await supabase.from("creator_applications").select("id,user_id,channel_name,legal_name,portfolio_url,rights_summary,status,submitted_at,reviewed_at,review_notes").eq("user_id",user.id).maybeSingle();return error?Response.json({error:"Could not load application"},{status:500}):Response.json({application:data},{headers:{"cache-control":"private, no-store"}})}
export async function POST(request:Request){if(!isSupabaseConfigured())return Response.json({error:"Cloud applications are unavailable"},{status:503});const supabase=await createClient();const{data:{user}}=await supabase.auth.getUser();if(!user)return Response.json({error:"Authentication required"},{status:401});const parsed=schema.safeParse(await request.json().catch(()=>null));if(!parsed.success)return Response.json({error:"Complete every required field before submitting"},{status:400});const{data,error}=await supabase.rpc("submit_creator_application",{p_channel_name:parsed.data.channelName,p_legal_name:parsed.data.legalName,p_portfolio_url:parsed.data.portfolioUrl,p_rights_summary:parsed.data.rightsSummary});return error?Response.json({error:error.message},{status:409}):Response.json({application:data},{status:201})}
