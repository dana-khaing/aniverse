import{createClient}from"@/lib/supabase/server";import{getAdminClient}from"@/lib/supabase/admin";import{isSupabaseConfigured}from"@/lib/supabase/config";import{profileReviewSchema}from"@/lib/public-profile";
export async function GET(_:Request,{params}:{params:Promise<{username:string}>}){if(!isSupabaseConfigured())return Response.json({error:"Cloud profiles are unavailable"},{status:503});const{username}=await params;const admin=getAdminClient();const{data:profile}=await admin.from("profiles").select("id,username,display_name,bio,avatar_path,profile_public").ilike("username",username).maybeSingle();if(!profile||!profile.profile_public)return Response.json({error:"Profile not found"},{status:404});const[{data:reviews},{count:watched},{count:lists},{data:activity}]=await Promise.all([admin.from("profile_reviews").select("id,title_name,body,score").eq("user_id",profile.id).order("created_at",{ascending:false}),admin.from("watch_progress").select("episode_id",{count:"exact",head:true}).eq("user_id",profile.id).eq("completed",true),admin.from("custom_lists").select("id",{count:"exact",head:true}).eq("user_id",profile.id),admin.from("watch_progress").select("completed,last_watched_at,episodes(number,seasons(titles(name)))").eq("user_id",profile.id).order("last_watched_at",{ascending:false}).limit(8)]);return Response.json({profile:{username:profile.username,name:profile.display_name||profile.username,bio:profile.bio||"Animation explorer",watched:watched??0},reviews:(reviews??[]).map(item=>({id:item.id,title:item.title_name,body:item.body,score:item.score})),achievements:[{name:"Season Pioneer",detail:`Completed ${watched??0} episodes`},{name:"Story Curator",detail:`Created ${lists??0} lists`},{name:"Trusted Reviewer",detail:`Published ${reviews?.length??0} reviews`}],activity:(activity??[]).map(item=>`${item.completed?"Completed":"Watched"} ${(item.episodes as unknown as {number:number;seasons:{titles:{name:string}}}).seasons.titles.name} episode ${(item.episodes as unknown as {number:number}).number}`)},{headers:{"cache-control":"public, max-age=60"}})}
export async function POST(request:Request,{params}:{params:Promise<{username:string}>}){if(!isSupabaseConfigured())return Response.json({error:"Cloud profiles are unavailable"},{status:503});const supabase=await createClient();const{data:{user}}=await supabase.auth.getUser();if(!user)return Response.json({error:"Authentication required"},{status:401});const{username}=await params;const admin=getAdminClient();const{data:profile}=await admin.from("profiles").select("id").ilike("username",username).maybeSingle();if(!profile||profile.id!==user.id)return Response.json({error:"You can only publish reviews on your own profile"},{status:403});const parsed=profileReviewSchema.safeParse(await request.json().catch(()=>null));if(!parsed.success)return Response.json({error:"Invalid review"},{status:400});const{data,error}=await admin.from("profile_reviews").insert({user_id:user.id,title_name:parsed.data.title,body:parsed.data.body,score:parsed.data.score}).select("id,title_name,body,score").single();return error?Response.json({error:"Review could not be published"},{status:500}):Response.json({review:{id:data.id,title:data.title_name,body:data.body,score:data.score}},{status:201})}
