import Link from "next/link";import { notFound } from "next/navigation";import { PublicHeader } from "@/components/catalog/public-header";import { locales,messages,type Locale } from "@/lib/i18n";
export default async function LocaleHome({params}:{params:Promise<{locale:string}>}){const {locale}=await params;if(!locales.includes(locale as Locale))notFound();const copy=messages[locale as Locale];return <><PublicHeader locale={locale as Locale}/><main className="locale-home"><p>{copy.eyebrow}</p><h1>{copy.title}</h1><span>{copy.copy}</span><Link href={`/${locale}/browse`}>{copy.cta}</Link><nav aria-label="Language"><Link href="/en">English</Link><Link href="/ja" lang="ja">日本語</Link></nav></main></>}
