import Link from "next/link";import {CalendarDays,Clock3} from "lucide-react";import {PublicHeader} from "@/components/catalog/public-header";import {listCatalog} from "@/lib/catalog-repository";
export default async function SchedulePage(){const catalog=await listCatalog();const days=["Monday","Tuesday","Wednesday","Thursday","Friday","Saturday","Sunday"];return <><PublicHeader/><main className="catalog-page"><div className="catalog-title"><p>NEVER MISS AN EPISODE</p><h1>Release schedule</h1><span>Times shown in your local timezone.</span></div><section className="week-grid">{days.map((day,di)=><div className={di===2?"today":""} key={day}><header><CalendarDays size={16}/><div><span>{day}</span><b>JUL {8+di}</b></div></header>{catalog.filter((_,i)=>i%7===di).map((t,i)=><Link href={`/anime/${t.slug}`} key={t.slug}><Clock3 size={14}/><div><h3>{t.name}</h3><p>Episode {t.episodes+i+1}</p></div><time>{18+i}:30</time></Link>)}</div>)}</section></main></>}
