"use client";
import { useCallback,useState } from "react";import { Send,Users } from "lucide-react";import { type PartyEvent,usePartyTransport } from "@/lib/realtime-party";
export function WatchParty({partyId}:{partyId:string}){const [messages,setMessages]=useState<PartyEvent[]>([{id:"welcome",author:"AniVerse",body:"Party created. Playback sync is ready."}]);const [body,setBody]=useState("");const receive=useCallback((message:PartyEvent)=>setMessages((current)=>current.some((item)=>item.id===message.id)?current:[...current,message]),[]);const {mode,send:sendEvent}=usePartyTransport(partyId,receive);async function send(event:React.FormEvent){event.preventDefault();if(!body)return;const message={id:crypto.randomUUID(),author:"Dana",body,type:"chat" as const};setMessages((current)=>[...current,message]);await sendEvent(message);setBody("");}return <main className="party-page"><header><div><p>WATCH PARTY · ECHOES OF ASTERIA</p><h1>Asteria finale night</h1><small>{mode==="cloud"?"Cross-device Supabase Realtime":"Same-browser local mode"}</small></div><span><Users/>3 watching</span></header><section><div className="party-screen"><b>EA</b><button onClick={()=>void sendEvent({id:crypto.randomUUID(),author:"Host",body:"Playback synchronized",type:"playback",sequence:Date.now()})}>Sync playback</button></div><aside><h2>Group chat</h2><div>{messages.map((message)=><p key={message.id}><b>{message.author}</b>{message.body}</p>)}</div><form onSubmit={send}><input aria-label="Party message" value={body} onChange={(event)=>setBody(event.target.value)} placeholder="Message the party"/><button aria-label="Send"><Send/></button></form></aside></section></main>}
